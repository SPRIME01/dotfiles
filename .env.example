# Environment configuration for the dotfiles project
# Copy this file to `.env` and fill in the values appropriate for your environment.

# Model Context Protocol (MCP) service configuration
# MCP_GATEWAY_URL=https://mcp.example.com/api
# MCP_ADMIN_USERNAME=your-username
# MCP_ADMIN_PASSWORD=your-password
# MCPGATEWAY_BEARER_TOKEN=your-bearer-token
# MCP_JWT_TOKEN=your-jwt-token

# Additional environment variables can be declared here.  Lines starting with '#' are comments.

# To use these values in your shell, ensure that your shell configuration calls the
# `load_env_file` function defined in `scripts/load_env.sh` before exporting
# environment variables.  Do not commit the `.env` file to version control.