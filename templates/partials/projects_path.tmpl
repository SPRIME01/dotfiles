{{/* Common projects path setup */}}
# Path for common projects (customize in a separate file if desired)
if [[ -d "$HOME/projects" ]]; then
  case ":$PATH:" in
    *":$HOME/projects/bin:"*) ;; # already present
    *) export PATH="$HOME/projects/bin:$PATH" ;;
  esac
fi
